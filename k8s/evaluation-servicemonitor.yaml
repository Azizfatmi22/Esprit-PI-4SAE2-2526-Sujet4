apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: evaluation-monitor
  labels:
    release: monitoring
spec:
  selector:
    matchLabels:
      app: evaluation
  endpoints:
    - port: http
      path: /actuator/prometheus
      interval: 15s
